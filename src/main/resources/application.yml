server:
  port: 8080
  servlet:
    context-path: /dynamicslab

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: yijunlin0621@gmail.com
    password: gmaj ksxo mgdj glgh
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    #    database-platform: com.oocl.demo.commonlib.jpa.MySQL8DialectCustomUtf8mb40900AsCs
    properties:
      hibernate:
        dialect:
          storage_engine: innodb
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: "jdbc:mariadb://localhost:3306?user=root&password=mysql2022&allowPublicKeyRetrieval=true&database=laboratory"
    driver-class-name: org.mariadb.jdbc.Driver


